doctype html 
html  
    head
        link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' rel='stylesheet')
        title='VIsta de productos'
    body
      div(class='container mt-3')
        section(class='jumbotron')
          h1 Vista Productos
          if productosExists 
             div(class='table-responsive')
               table(class='table')
                 thead(class='thead-light')
                   th(scope="col") Id
                   th(scope="col") Nombre
                   th(scope="col") Precio
                   th(scope="col") Foto
                 tbody
                   for item in productos
                      tr
                        th(scpe='row') #{item.id}
                        td #{item.title}
                        td #{item.price}
                        td
                          img(width='50', src=item.thumbnail, alt=item.title, title=item.title)
          else 
              h3(class='alert alert-warning') No se encontraron productos
          a(href='/', type='button', class='btn btn-primary') Cargar un nuevo producto